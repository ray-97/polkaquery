{
  "name": "staking_era_stat_list",
  "description": "Era stat list",
  "api_path": "/api/scan/staking/era_stat",
  "api_method": "POST",
  "parameters": {
    "type": "object",
    "properties": {
      "address": {
        "type": "string",
        "description": ""
      },
      "page": {
        "type": "integer",
        "description": "Page number, starting from 0",
        "minimum": 0,
        "example": 0
      },
      "row": {
        "type": "integer",
        "description": "Data size per page",
        "minimum": 1,
        "maximum": 100,
        "example": 10
      }
    },
    "required": [
      "address"
    ]
  },
  "response_schema_description": "Returns JSON data for Era stat list."
}