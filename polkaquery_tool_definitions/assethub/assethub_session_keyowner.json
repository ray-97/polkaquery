{
    "name": "assethub_session_keyowner",
    "description": "The owner of a key. The key is the `KeyTypeId` + the encoded key.",
    "client_type": "rpc",
    "pallet_name": "Session",
    "storage_item_name": "KeyOwner",
    "parameters": {
        "type": "object",
        "properties": {
            "key1": {
                "type": "integer",
                "description": "Parameter of type ('[u8; 4]', 'Bytes')"
            }
        },
        "required": [
            "key1"
        ]
    }
}