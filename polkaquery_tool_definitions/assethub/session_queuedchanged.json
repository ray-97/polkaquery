{
    "name": "session_queuedchanged",
    "description": "True if the underlying economic identities or weighting behind the validators  has changed in the queued validator set.",
    "client_type": "rpc",
    "pallet_name": "Session",
    "storage_item_name": "QueuedChanged",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}