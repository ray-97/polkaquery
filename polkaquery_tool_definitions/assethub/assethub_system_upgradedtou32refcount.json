{
    "name": "assethub_system_upgradedtou32refcount",
    "description": "True if we have upgraded so that `type RefCount` is `u32`. False (default) if not.",
    "client_type": "rpc",
    "pallet_name": "System",
    "storage_item_name": "UpgradedToU32RefCount",
    "parameters": {
        "type": "object",
        "properties": {},
        "required": []
    }
}