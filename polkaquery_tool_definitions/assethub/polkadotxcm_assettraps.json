{
    "name": "polkadotxcm_assettraps",
    "description": "The existing asset traps.   Key is the blake2 256 hash of (origin, versioned `Assets`) pair. Value is the number of  times this pair has been trapped (usually just 1 if it exists at all).",
    "client_type": "rpc",
    "pallet_name": "PolkadotXcm",
    "storage_item_name": "AssetTraps",
    "parameters": {
        "type": "object",
        "properties": {
            "key1": {
                "type": "string",
                "description": "Parameter of type scale_info::13"
            }
        },
        "required": [
            "key1"
        ]
    }
}