[pytest]
# Set the default event loop scope for pytest-asyncio fixtures
# Recommended value is 'function' to align with future defaults and ensure test isolation.
asyncio_default_fixture_loop_scope = function
